目标:用官方 eval/example/example_test_submission.jsonl 作为"参考真值输出",定位我的复现与官方发布之间的一切差异(代码约定、推理配置、数据、分数分布),判断我的"脆弱性"结论是否成立。

第0步(决定性对照,先做这个):
- 用我自己的 eval/eval_main.py,对官方 example_test_submission.jsonl 在 --gmiou_cls_threshold ∈ {0.4,0.6,0.8} 各跑一次,
  命令:python eval/eval_main.py --submission_path eval/example/example_test_submission.jsonl --gt_path data/label/Standard/test.jsonl --save_path <out> --gmiou_cls_threshold <τ>
- 把官方 submission 的 G-mIoU@1 三点曲线与"我复现模型"的曲线、以及论文 Table 8 三列并排列出。
- 判读:
  (a) 若官方 submission 在 τ=0.4 也坍塌(个位数)→ 证明"脆弱"是 Moment-DETR-GMR 固有属性,我的复现无缺陷,论文 Table 8 的 35.84 才是异常点;
  (b) 若官方 submission 在 τ=0.4 ≈35 → 我的训练确实跑偏,继续做下面各步定位原因。

第1步(存在性分数分布对比 —— 最能说明问题):
- 分别对"我的 submission"和"官方 submission",按场景(single / multi / null-set,依据 test.jsonl 的 relevant_windows 长度)画 pred_exist_score 直方图。
- 重点核对:single 是否≈1.0;multi 与 null-set 是否重叠在 0.3–0.55。两份分布形状是否一致。
- 若我的分布相比官方整体右移/左移或重叠更严重 → 这就是 τ 敏感性差异的根因,记入 score_drift_analysis.md。

第2步(阈值约定的代码级差异,逐一核对):
- eval/metrics.py:compute_gmr_cls 用 score > thd;prepare_submission_for_gmiou 用 score > cls_threshold(严格大于)。
- training/moment_detr_gmr/evaluate.py:apply_existence_gate 用 exist_scores >= threshold(大于等于),且 exist_gate_thd 默认 0.4。
- 确认我复现时报告的数,是经 eval_main.py(离线、> 约定)算的,还是经 evaluate.py 推理期门控(>= 约定、且会改写窗口分数)算的。两条路径阈值约定不一致,边界样本会被分到不同类。

第3步(推理配置差异):
- 核对我生成 submission 时的 exist_gate_thd 取值、是否用了 --hard_exist_gate。
- 注意 evaluate.py 的 soft gate 会把窗口分数乘以 exist(当 exist<τ 时),从而改写保存到 submission 的 window score;官方 example 的窗口分数看起来是未被门控改写的原始前景概率。
  → 对照我的 submission 窗口分数是否被乘缩过(同一 qid 下窗口分数是否系统性低于官方),这会影响 mAP/mR 的排序与数值,但不影响 AUROC。

第4步(数据与划分差异):
- 确认我用的 GT 是 data/label/Standard/test.jsonl(而非 Full),且与官方 submission 的 qid 完全对齐;
- 打印 eval_main.py 日志里的 shared / gt_only / pred_only 三个计数,确认没有因 qid 缺失被悄悄丢样本。

产出:一张"差异对照表"(第0步三点曲线 + 第1步分布图 + 第2/3步配置/约定差异 + 第4步样本计数),并明确写出最终判定属于 (a) 还是 (b)。
