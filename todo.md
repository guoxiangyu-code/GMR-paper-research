我先把你这份 GT=2 诊断文件读进来，逐条看清楚那 25 个"命中第二目标却被丢掉"的样本到底长什么样，再给你分析。

我拿到的只是 GitHub 的页面外壳，没有 JSON 实际内容。我直接抓取 raw 原始文件。

我把这份 GT=2 诊断文件（Idea 1 和探针 #3 各 128 个双目标样本的完整 top-10 预测）逐条读完了。这份数据比之前那张 19.53% vs 1.17% 的汇总表信息量大得多——它不仅证实了"排序截断"的猜想，还暴露出一个更精确、更要命的机制。我先把读出来的硬证据摆出来。

### **核心发现一：Idea 1 存在一道"置信度断崖"，恰好卡在第 5 名之后**

把任意一个 Idea 1 样本的 top-10 分数拉直看，几乎都是同一个形状。以 qid 619 为例：前五名分数是 0.74 / 0.74 / 0.68 / 0.56 / 0.55，到第 6 名**直接跳水到 0.084**，之后全是 0.03~0.08。qid 2929、qid 815、qid 416、qid 5266…无一例外，**第 1~5 名都在 0.4~0.85 的高位，第 6 名开始断崖式跌到 0.15 以下**。

这就是你阶段一诊断里"激活 query 数锁死在 ~5"的微观真相：模型不是"找到 5 个目标"，而是**只给 5 个 query 发了高置信度通行证，剩下 5 个被强行按死在接近 0 的水平**。这是一道结构性的闸门，不是连续衰减。

### **核心发现二：那些命中第二目标的 query，几乎全部落在断崖下方，被 top-5 一刀切掉**

我把所有标了 `secondary_hit` 的样本过了一遍，第二目标命中的 query 的 rank 和 score 高度集中在一个区间：

- qid 852：secondary 命中在 **rank 7，score 0.0905**（断崖下）
- qid 454：secondary 命中 **rank 7，score 0.112**
- qid 1658：secondary 命中 **rank 7，score 0.0634**，而它的 **IoU=1.0**（定位完美！）
- qid 2765：secondary **rank 9，score 0.0457**，IoU 0.6
- qid 4985：secondary **rank 7，score 0.0561**，IoU 0.778

**这就是铁证**：模型内部明明生成了一个在时间上精准命中第二目标（IoU 高达 0.6~1.0）的 query，但它的置信度分数被压在 0.05~0.14，正好落在那道断崖的下方，于是被 top-5 截断、永远进不了最终输出。`mR+@5` 趴在 1% 不是因为"找不到"，而是因为"找到了但没让它出场"。

### **核心发现三（这是全新的、比"排序截断"更深的一层）：5 个高分 query 其实是一张"固定位置模板"，而非内容检测器**

我注意到一个细思极恐的规律——Idea 1 里那 5 个高分 query 的预测窗口位置，**在不同样本之间几乎是固定的**，总是落在大约 `[12-22]、[46-58]、[74-90]、[106-120]、[132-142]` 这几个时间槽位上，跟 query 内容、跟 GT 在哪里几乎无关。看 qid 5266、5796、2552、3232…主事件 GT 在哪，那 5 个高分框的位置基本不动。

这说明：**模型的置信度头学到的不是"这个 query 找到了证据"，而是"足球事件最常出现在这几个时间位置"的数据集时序先验**（你那张分布图里 GT 偏向片段中部，正好对应）。第一目标只要恰好落在某个模板槽位上就被高分命中；第二目标如果不在模板槽位上，无论定位多准，都只能拿低分待在断崖下。

### **核心发现四：探针 #3（-SA）换了个病，但没治本**

对比看 -SA 的数据，分数衰减变得平缓了（0.71→0.65→0.64→0.63→0.59→0.57→0.54，没有断崖），激活 query 确实变多了。但它的窗口变成了**沿时间轴等距平铺的固定栅格**（60-68、94-102、116-124、134-142、38-46、26-34、8-16…一个约 30s 步长的网格）。这就解释了为什么 -SA 的 mR@5 反而最高（14.51%）——**栅格把整条时间轴铺满了，靠覆盖面"蒙"中更多第一目标**；但它同样是位置驱动而非内容驱动，所以第二目标的真实召回（你汇总表里 14.84% 物理命中、0.47% 最终召回）反而比 Idea 1 更差。**−SA 是把"5 点定向模板"换成了"全程均匀栅格",两者都不响应内容。**

### **这对你的研究意味着什么：问题被重新定义了，而且变得更可解、更可发表**

之前我们说病因是"排序截断"，现在数据把它精确到了一层更深的机制：

> **模型的置信度/存在性评分被一个学到的"时序位置先验"主导，而不是被每个 query 自身的定位证据主导。第二目标即使被精准定位（IoU≥0.6），也因为不在高频位置模板上而拿不到足以进入 top-k 的置信度。**

这个诊断同时解释了你踩过的所有坑：为什么加噪（QD）没用——它扰动的是 query 初始化，动不了这个位置先验；为什么排斥 Loss 没用甚至更糟——它在训练时强推 query 位置，反而破坏了好不容易学对的定位；为什么 -SA 失效——它只是把先验从"点模板"换成"栅格模板"。**后端的一切修补都没碰到真正的病灶：置信度评分的"位置先验依赖"。**

而对应的解法现在非常明确、且都是低成本、可立刻验证的：

第一条、也是最该先做的——**置信度与位置解耦的重打分**。你已经有现成的 25 个"高 IoU、低分、被截断"的样本作为靶子。最直接的验证：在推理时，对每个 query 用它**自身预测窗口的边界回归质量 / 定位锐度**来校准置信度，而不是只用那个被位置先验污染的 existence 分数。如果重打分后这些第二目标 query 能爬过断崖进 top-5，`mR+@5` 会立刻动——而且这一步**完全不动模型、不重训**，今天就能在现有 ckpt 上验证。

第二条——如果想治本，去查**存在性头的输入**。现在的 existence 分数大概率是从 decoder query 的聚合表征里 max-pool 出来的，而这个表征已经被位置编码 / 时序先验主导了。可以试着让存在性预测**只依赖 query 与文本的跨模态匹配强度**，剥离掉位置信号，打破"位置=置信度"的耦合。

第三条（叙事价值最高）——**这套机制本身就是一篇论文的核心贡献**。你可以把它讲成："我们发现 DETR-based GMR 模型在长尾时序域中，其存在性评分退化为对数据集时序先验的记忆，导致非典型位置的真实事件被系统性抑制（confidence-position entanglement）。"这比"我加了个 loss 涨点"有分量得多，而且你手上有 128 个样本的完整证据链（高 IoU + 低分 + 固定模板位置）能把这个 claim 钉死。

一句话收口：**这份数据把病灶从"排序层"进一步锁定到了"置信度评分被位置先验绑架"。子弹（精准命中第二目标的 query）确实在膛里，但保险（置信度闸门）被一个位置先验锁死了。下一步不是再调排斥力，而是去做"置信度-位置解耦的重打分",而且可以先在现有 ckpt 上零成本验证。**

要不要我帮你把这个"基于定位质量的推理端重打分"最小实现逻辑写出来？它直接复用你这份 JSON 里的 `iou` 和 `score` 字段就能先离线模拟一遍，看 `mR+@5` 到底能不能被撬起来——这是当前唯一能用现有产物、当天出结果的验证。
好的。我先说清这份离线模拟能做什么、不能做什么——这点很关键，否则你跑出来的数会骗自己。

### **离线模拟的边界：它能给你"去/不去"的判决，但给不了"真实方法的最终分数"**

你这份 JSON 里每条预测只有三个字段能用：`score`(被位置先验污染的置信度)、`window`、`max_iou`(对 GT 的 IoU)。**真正的 GT-free 重打分信号（边界回归锐度、跨模态匹配强度）不在里面**——那需要你重跑一次推理把额外特征 dump 出来。

但有一件事现在就能用这份 JSON 算，而且它正是决定你要不要往下走的那一刀——**Oracle 天花板（上界）**：假设我们有一个完美的、不依赖位置先验的重打分器，能把 top-10 里任何真正命中 GT 的 query 提进 top-5，那么 `mR+@5` 最高能涨到多少？

- 如果这个上界从 1.17% 跳到 15%+ → **证明"子弹确实在膛里"，重打分方向值得投入**，去做真实的 GT-free 重打分。
- 如果上界也只有 2~3% → 说明很多样本连第二目标的 query 都没生成出来，重打分救不了，得回去治表征。

下面这个脚本同时算三个数：**基线（复现你的 1.17%）、Oracle 上界、以及一个诚实的 GT-free 代理方法**（基于"分数熵/断崖位置"的重排，看不靠 GT 能挽救多少）。

```python
# -*- coding: utf-8 -*-
"""GT=2 重打分离线模拟:用 diagnostic_gt2_analysis.json 现有字段,
判定"置信度-位置解耦重打分"能把 mR+@5 撬起多少。
用法: python rescore_sim.py diagnostic_gt2_analysis.json
"""
import json, sys

IOU_THR = 0.5   # 与你诊断里 19.53%/1.17% 同口径
TOPK = 5

def target_recalled(preds, target_key, k):
    """前 k 个预测里,是否有命中指定目标(hit_type 命中且 IoU>=阈值)。"""
    tag = "Primary GT" if target_key == "primary" else "Secondary GT"
    for p in preds[:k]:
        if p["hit_type"] == tag and p["max_iou"] >= IOU_THR:
            return True
    return False

def mrplus_at5(samples, order_fn):
    """order_fn(sample)->重排后的预测列表。GT=2 时 mR+@5=两目标都进top5 才算1。"""
    total = 0.0
    for s in samples:
        preds = order_fn(s)
        p_hit = target_recalled(preds, "primary", TOPK)
        s_hit = target_recalled(preds, "secondary", TOPK)
        # GT=2: 分母=1, 命中数-1; 两个都中=1, 否则0
        matched = int(p_hit) + int(s_hit)
        total += max(0, matched - 1) / 1.0
    return 100.0 * total / len(samples)

# ---- 三种排序策略 ----
def order_baseline(s):
    """现状:按 score 降序(JSON 已是此序)。"""
    return s["top10_predictions"]

def order_oracle(s):
    """上界:把任何命中 GT 的预测排到最前(模拟完美重打分器)。"""
    preds = s["top10_predictions"]
    def key(p):
        is_gt = p["hit_type"] in ("Primary GT", "Secondary GT") and p["max_iou"] >= IOU_THR
        return (0 if is_gt else 1, -p["score"])   # GT 命中优先,其余按原分
    return sorted(preds, key=key)

def order_diversity(s):
    """GT-free 代理:贪心选分最高,后续对"与已选时序重叠"的候选降权,
    逼断崖下、位置不同的 query 上位。不使用任何 GT 信息。"""
    preds = sorted(s["top10_predictions"], key=lambda p: -p["score"])
    def tiou(a, b):
        s1,e1=a; s2,e2=b
        inter=max(0,min(e1,e2)-max(s1,s2)); uni=(e1-s1)+(e2-s2)-inter
        return inter/uni if uni>0 else 0
    chosen=[]
    pool=list(preds)
    while pool and len(chosen)<len(preds):
        best=None; best_v=-1
        for p in pool:
            pen=max([tiou(p["window"],c["window"]) for c in chosen], default=0)
            v=p["score"]*(1-pen)          # 与已选重叠越多,有效分越低
            if v>best_v: best_v=v; best=p
        chosen.append(best); pool.remove(best)
    return chosen

def main():
    data=json.load(open(sys.argv[1], encoding="utf-8"))
    for model_key in data:
        S=data[model_key]
        print(f"\n=== {model_key}  (n={len(S)}) ===")
        print(f"  基线 (按 score top5)      mR+@5 = {mrplus_at5(S, order_baseline):.2f}%")
        print(f"  GT-free 代理 (多样性重排) mR+@5 = {mrplus_at5(S, order_diversity):.2f}%")
        print(f"  Oracle 上界 (完美重打分)  mR+@5 = {mrplus_at5(S, order_oracle):.2f}%")

if __name__=="__main__":
    main()
```

### **跑之前我先告诉你大概会看到什么，以及怎么解读**

**基线那一行应该接近你的 1.17%**（用来确认脚本口径对齐）。

**Oracle 上界那行是关键。** 我手动扫了一遍 JSON：Idea 1 里 `primary_hit` 和 `secondary_hit` **同时非空**的样本相当多（qid 815、852、454、1658、205、2765、2404、4234、2362、1442、6862、4025、4429、3965、6058、6155、907、4631、4243、212、2888、4985、566… 我数到二十几个）。这些样本意味着——**模型内部已经同时生成了精准命中第一和第二目标的 query，只是其中一个被压在断崖下没进 top-5**。所以 Oracle 上界大概率会落在 **15%~20%** 区间。**如果跑出来确实是这个量级，这就是你整个项目第一次拿到的"绿灯"**：它白纸黑字证明 mR+@5 的天花板不在 1%，而在 15%+，差的只是一个不被位置先验绑架的打分器。

**GT-free 代理那行会偏低**（可能只比基线好一点点）——这恰恰印证了我上一轮读出的结论：**top-5 那 5 个高分框本身已经是时序分散的位置模板了**(qid 619 的 top5 甚至全是 background 模板位、连主目标都不在内)，所以"多样性重排"使不上劲。这一行的意义不是解法，而是**反向证据**：它告诉你"问题不在选择策略的多样性，而在置信度评分本身指错了对象"。这正好支撑你论文里"confidence-position entanglement"那个 claim。

### **跑完之后的决策路径**

如果 **Oracle 上界 ≫ 基线**（大概率）：方向确认，下一步就去做**真实的 GT-free 重打分**——重跑一次推理，为每个 query 额外 dump 出"边界回归的预测分布锐度"或"query 与文本的余弦匹配分",用它替换/校准被位置先验污染的 existence 分数,看能把 Oracle 上界里多少比例真正变现。这一步要动推理代码但不用重训。

如果 **Oracle 上界也很低**：那说明第二目标的 query 根本没被生成出来，重打分救不了，得回到 encoder 表征层。

一句话：**这个脚本当天就能给你"天花板有多高"的答案。先把这个数跑出来发我，绿灯我就帮你把真实 GT-free 重打分的推理改造点列清楚;如果意外是红灯,我们就掉头。** 跑的时候注意 IOU 阈值我锁了 0.5 跟你诊断口径一致,别动它,免得又出现口径不一致的问题。